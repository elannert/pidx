#!/bin/bash
# use this once: git config credential.helper store
git add *
git commit -m "removed what you want to be when grow up - this created duplicate values and was not helpful for password retrieval either"
git push origin master